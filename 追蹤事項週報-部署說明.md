# 環保部門週報 ENV WEEKLY REPORT — 部署與使用說明

每週一 09:00 自動寄出、每週五 15:00 提醒更新照片。取代原本手動製作的週報。

對應檔案：
- `esystem-gas/Code.gs`：Environmental E-System 的 Apps Script 後端（週報模組在檔案後段；`更新.bat` 會自動 clasp push + deploy）
- `index.html`：前端「📧 週報設定」視窗
- `tpc-pipeline-improvement-system_30.html`：改善單系統，「匯出總結」會自動把 KPI 圖同步到週報

## 1. 更新 Apps Script（自動）

後端程式在 `esystem-gas/Code.gs`，用 clasp 綁定 Apps Script 專案（`esystem-gas/.clasp.json`）。
**跑 `更新.bat` 就會自動處理**：commit → push GitHub → 若 `esystem-gas` 有變動就 `clasp push` + `clasp deploy`（更新同一個網頁應用程式網址的版本）。
不用再手動貼程式碼、不用進「管理部署作業」。

手動操作（有需要時）：
```
cd esystem-gas
clasp push -f
clasp deploy -i AKfycbyT6kFs6nCt6YCHyQjdbMxgErkOHNxwcIuncju0UkzBq4TvGOIigINusItj9bvcUbRrcw -d "說明"
```
若出現未登入錯誤，先執行 `clasp login`。

## 2. 授權 + 安裝排程（只有新增權限時要再做）

1. 函式下拉選 **`setupTrackerMailTrigger`** → ▶ 執行。
2. 授權視窗 → 允許。這版會要求：Gmail 寄信、Drive（照片與 KPI 圖）、Google 簡報（畫 KPI 圖）、連線外部服務（抓改善單資料）、翻譯。
   > 之前授權過也**必須再跑一次**，因為權限增加了。
3. 執行紀錄出現「✅ 已安裝觸發器」即完成。

| 觸發器 | 時間（Asia/Taipei） | 功能 |
|---|---|---|
| `weeklyTrackerMail` | 每週一 09:00 | 寄出 ENV WEEKLY REPORT |
| `fridayReportReminder` | 每週五 15:00 | 寄提醒信：更新現場照片（KPI 圖自動處理） |

> Google 時間觸發器有 ±15 分鐘誤差，這是平台限制。

編輯器裡可直接執行的測試函式：

| 函式 | 用途 |
|---|---|
| `testNcrSource` | 執行紀錄列出抓到的改善單，確認資料正確 |
| `testTrackerMailToMe` | 寄一封【測試】週報到你的 Google 帳號 |
| `testReminderMail` | 寄一封週五提醒信給設定的提醒收件者 |
| `testKpiSlide` | 用 Slides 畫一張 KPI 圖存到 Drive，執行紀錄印出網址 |
| `listTrackerMailTriggers` | 列出目前觸發器 |

## 3. 系統中的「📧 週報設定」

登入編輯模式 →「📋 追蹤事項」分頁 → **「📧 週報設定」**（或從週五提醒信的按鈕直接開啟）。

**① 收件者與排程**
- 收件者 To / 副本 CC：多位用逗號分隔
- 寄件者顯示名稱
- 週五提醒信收件者（預設 paul.tong@nmdc-group.com）
- 三個開關：每週一自動寄出、每週五提醒、中文自動加英文小字

**② 開頭文字**：簡易文字編輯器（粗體、斜體、底線、清單、顏色），預設是「Dear Chris, Sam … BR, Paul Tong」。「還原預設」可復原。

**③ 現場照片**：「＋ 新增照片」可一次選多張，上傳時會等比縮小、置中放進固定 16:9 畫框（1280×720，不裁切，空白處淺灰底）再存到 Drive 的 `EHS-Weekly-Report` 資料夾，信件裡每張等高；每張可填標題、地點、日期，並可上下排序或移除。

**④ 改善單 KPI 總結圖**：全自動，不用手動處理。來源有兩個：
- 改善單系統只要有人開啟「總覽儀表板」，就會在背景把 KPI 總結圖（跟「匯出總結」一模一樣的版型，原始解析度 3200×1800 PNG 不壓縮）同步到週報，每 6 小時最多一次，不下載檔案、不跳提示。
- 寄出時若本報告週內沒有同步過的圖，Apps Script 會用 Google Slides 依最新改善單資料自己畫一張（版型是簡化版：四個統計卡、分類統計長條、圓餅分佈）。
- 設定頁可按「🖼 由系統產生」先看系統畫出來的版型，也保留「手動上傳」。

底部按鈕：
- ✉️ 寄測試信：只寄給測試信箱（主旨加【測試】，不含 CC），會先自動儲存設定
- 👁️ 預覽版型：新分頁顯示信件（圖片改用 Drive 網址顯示，不含附件）
- 🔔 測試提醒信：立刻寄一封週五提醒信
- 📤 立即寄出週報：先儲存設定，再立刻寄給收件者
- ☁️ 儲存設定：排程讀的是雲端已儲存的設定，改完一定要存

原本「📧 寄出追蹤摘要」（複製到 Outlook）的視窗仍在，收件者改為讀週報設定，並多了「📤 由系統寄出」。

## 4. 信件內容與順序

主旨：`ENV WEEKLY REPORT (2026/09/07~2026/09/13)`，區間是「昨天所在那一週」的週一～週日：週一寄出＝上週一～上週日。

1. 頁首：環保部門週報 / ENV Weekly Report · Tracker & Improvement Notice Status，右側報告期間
2. 開頭文字（設定頁編輯）
3. KPI 卡片：改善單待處理、追蹤事項逾期、本週到期、排程中
4. 🛠️ 改善單狀態：標題下註明「📎 統計表如附件（改善單 KPI 總結圖）」，KPI 圖只作附件不內嵌；總計／未結案／已結案，已逾期與 7 天內到期表格（4 欄：編號、內容、期限、單位／開單人，手機也排得下）
5. 📋 追蹤事項：已逾期 → 本週到期 → 排程中（4 欄：優先、事項、負責人、期限），備註列在事項下方
6. 📅 本週活動：「重點活動」裡日期落在寄出日起 7 天內（週一寄＝週一～週日）的活動，顯示日期、類型、標題、工區／負責人、備註；沒有活動會顯示「本週沒有排定的活動」
7. 📸 現場照片：兩欄、固定 16:9 等高（上傳時已置中縮放），標題／地點／日期
8. 開啟 E-System 按鈕、頁尾

中文內容（事項、備註、改善單內容、單位、照片標題與地點）會自動用 Google 翻譯加上英文小字；譯文快取在 Google Sheet 的 `Translations` 工作表，可直接在該表修正譯文，下次寄出會採用修正後的版本。地名等固定用語不交給 Google 翻譯（`Code.gs` 的 `TR_TERMS`：通霄 → TungHsiao、台中 → Taichung、天力 → Tienli…），要加新的固定用語就改那個清單。

改善單資料來源是改善單系統的 GAS（`NCR_GAS_URL`），與公告欄同一套算法；不是舊的 `SyncData` 工作表。

## 5. 字型（不會是新細明體）

Outlook 桌面版用 Word 引擎排版，中文一律用「東亞字型」（預設新細明體）。本版型每個 `<td>` / `<p>` / `<a>` 都自帶 `Microsoft JhengHei` 為首的字型堆疊，加上 `mso-fareast-font-family` 等屬性，`<head>` 另有 `<!--[if mso]>` 樣式強制覆蓋。Gmail / Outlook 網頁版 / 手機依序用微軟正黑體 → PingFang TC → Noto Sans TC → Segoe UI。

## 6. 注意事項

- 寄件者是 **Apps Script 擁有者的 Google 帳號**。
- 每日寄信上限：一般 Gmail 100 封／Workspace 1500 封；單封信含附件上限 25MB（照片已壓縮，一般 10 張內沒問題）。
- 設定存在 Google Sheet `Data` 工作表：`B5`（收件者、開頭文字、提醒、照片清單）、`B6`（KPI 圖）。
- 移除的照片與被取代的 KPI 圖會移到 Drive 垃圾桶。
- **照片封存**：週報（排程或「立即寄出週報」）寄出成功後，這次寄出的照片會自動從設定頁移除，Drive 檔案搬到 `EHS-Weekly-Report/已寄出/<報告週>` 子資料夾；寄信時也只會放「上次寄出之後」上傳的照片，所以不會把上週的照片再寄一次。「寄測試信」不會封存。週一 09:00 之後上傳的照片會進下週一那份。
- 週五提醒信會顯示「本週已上傳 N 張」，0 張時用警告色。
