# 追蹤事項週報（每週一 09:00 自動寄送）— 部署步驟

對應檔案：`Code.gs`（Environmental E-System 的 Apps Script 後端）、`index.html`（前端）。

## 1. 更新 Apps Script

1. 開啟 `index.html` 裡 `GAS_URL` 指向的 Apps Script 專案（Google Sheet ID `12GwWG_…`那一個）。
2. 把編輯器裡的 `Code.gs` **全部刪除**，貼上本專案根目錄的 `Code.gs`（原本所有功能都保留，只是多了週報模組）。
3. 確認第一段 `SYSTEM_URL` 是系統實際網址（信件底部「開啟 E-System」按鈕用）。
4. 儲存。

## 2. 重新部署（保留同一個網址）

「部署」→「管理部署作業」→ 右上鉛筆 ✏️ →「版本」選 **新版本** → 部署。

> ⚠️ 不要按「新增部署作業」，那會產生新網址，前端就連不到了。

## 3. 授權 + 安裝排程（只需做一次）

1. 編輯器上方函式下拉選 **`setupTrackerMailTrigger`** → 按 ▶ 執行。
2. 跳出授權視窗 → 允許（會要求 Gmail 寄信與觸發器權限）。
3. 執行紀錄出現「✅ 已安裝觸發器：每週一 09:00（台北時間）」即完成。

排程明細：

| 觸發器 | 時間 | 功能 |
|---|---|---|
| `weeklyTrackerMail` | 每週一 09:00（Asia/Taipei） | 寄出未完成的追蹤事項 + 7 天內到期／逾期的改善單 |

> Google 時間觸發器有 ±15 分鐘誤差（可能 09:07 才送出），這是平台限制。
> 觸發器已指定台北時區，不受專案時區設定影響。

想先確認信長什麼樣：函式下拉選 **`testTrackerMailToMe`** → ▶ 執行，會寄一封【測試】到你的 Google 帳號信箱。

## 4. 在系統中設定收件者

登入編輯模式 →「📋 追蹤事項」分頁 → **「⚙️ 週報寄送設定」**：

- **啟用每週一自動寄送**：取消勾選 = 暫停排程寄信（觸發器不用移除）
- **收件者 To**：多位用逗號分隔
- **副本 CC**：選填
- **寄件者顯示名稱**：預設 `NMDC Environmental E-System`
- 按 **「☁️ 儲存設定」**（排程讀的是雲端已儲存的設定，一定要存）

尚未儲存過時，會預先帶入原本寫死的收件者（Raymond / Sean / Jacqueline，CC EHS group）。

同一個視窗還有：

- **✉️ 寄測試信**：只寄給你填的測試信箱（不會寄給收件者、不含 CC，主旨加【測試】）
- **👁️ 預覽版型**：新分頁顯示信件內容，不寄出
- **📤 立即寄出週報**：先儲存設定，再立刻寄給收件者

原本「📧 寄出追蹤摘要」的預覽視窗也多了 **「📤 由系統寄出」**（同樣寄給設定頁的收件者），且「開啟 Outlook」帶入的收件者改為讀設定頁。

## 5. 信件內容

- 頁首：NMDC Energy｜Environmental E-System，製表日期
- 狀態橫幅：待辦數／逾期數／本週到期數（有逾期時紅色、無逾期時琥珀色、全部完成時綠色）
- 四格統計：已逾期、本週到期、排程中、改善單待處理
- 追蹤事項：依「已逾期 → 本週到期 → 排程中」分表，表內依優先度、期限排序，備註以純文字列在事項下方
- 改善單：已逾期、7 天內到期（來源同公告欄的改善單資料）
- 「開啟 E-System」按鈕 + 頁尾說明

## 6. 字型（不會是新細明體）

Outlook 桌面版用 Word 引擎排版，中文一律用「東亞字型」（預設新細明體）。本版型：

- 每個 `<td>` / `<p>` / `<a>` 都自帶 `Microsoft JhengHei` 為首的字型堆疊
- 加上 `mso-fareast-font-family` / `mso-ascii-font-family` / `mso-hansi-font-family`
- `<head>` 另有 `<!--[if mso]>` 樣式強制覆蓋

Gmail / Outlook 網頁版 / 手機會依序用 微軟正黑體 → PingFang TC → Noto Sans TC → Segoe UI。

## 7. 注意事項

- 寄件者是 **Apps Script 擁有者的 Google 帳號**。
- 每日寄信上限：一般 Gmail 100 封／Workspace 1500 封。
- 收件者設定存在 Google Sheet `Data` 工作表的 `B5` 儲存格（JSON）。
- 查看目前觸發器：執行 `listTrackerMailTriggers`；重複執行 `setupTrackerMailTrigger` 不會產生重複觸發器。
